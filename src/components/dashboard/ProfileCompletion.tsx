import React from 'react';
import StatCard from './StatCard';

const ProfileCompletion: React.FC = () => {
  return <StatCard title="Profile Completion" value="80%" />;
};

export default ProfileCompletion;
